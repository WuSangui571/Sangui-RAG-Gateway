Sangui RAG Gateway provides an OpenAI-compatible API gateway integration style.
Existing business systems can replace the original LLM base URL and API key with the gateway endpoint and app API key.
The gateway adds private knowledge retrieval, prompt augmentation, upstream forwarding, and request observability.